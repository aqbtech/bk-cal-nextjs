/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // swcMinify: true,
  
  // // Support both App Router (/app) and Pages Router (/pages)
  // experimental: {
  //   appDir: true,
  // },
};

module.exports = nextConfig;
